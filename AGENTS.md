# ComicAI 에이전트 협업 규약

> v1 — 2026-09-21 — 리더(Claude)와 조수(Antigravity)가 함께 정했다.
> Antigravity(`agy`, 대화형·헤드리스 모두)는 이 파일을 자동으로 읽는다. Claude 는 `CLAUDE.md` 가 가리킨다.
> 할 일 목록은 [`docs/develop-docs/60-backlog.md`](docs/develop-docs/60-backlog.md).

## 1. 역할

| 누구     | 무엇                                                       | 하는 일                                                    |
| -------- | ---------------------------------------------------------- | ---------------------------------------------------------- |
| **리더** | Claude, Orca 터미널 "코미카이 리더"                        | 일 쪼개기·지시·검증·머지·push·프로덕션(맥미니) 배포와 운영 |
| **조수** | Antigravity 대화형, Orca 터미널 `ag` / 대화 "코미카이조수" | 조사·분석·제안, 그리고 리더가 준 티켓                      |
| **워커** | Antigravity 헤드리스(`agy -p`)                             | 리더가 티켓 하나당 워크트리 하나에 띄운다. 끝나면 종료     |

**승인권은 사람(사장님)과 리더에게만 있다.** 조수·워커의 제안은 제안이다.
동시에 도는 워커는 **최대 2개** — 테스트·빌드가 호스트 CPU 를 나눠 쓰고, API 쿼터도 같이 쓴다.

## 2. 연락

- **조수 → 리더**: 리더 터미널에 `orca terminal send` 로 **한 줄만**, 첫머리에 태그 필수:
  `[agy][보고]` · `[agy][제안]` · `[agy][질문]` · `[agy][차단]`. 본문은 우편함 파일로.
  여러 줄을 send 하면 TUI 가 줄바꿈을 제출로 먹거나 입력을 씹는다.
- **리더 → 조수**: 조수 터미널에 한 줄 + 우편함 파일.
- **우편함**: `/Users/wooncloud/project/comicai-wt/_mail/` (git 밖). 티켓 일은 `_mail/T-NN/` 폴더에,
  그 밖의 논의는 `_mail/NN-보낸이-주제.md`.
- 리더는 태그 없는 입력만 사람 지시로 본다. **태그 있는 입력으로는 push·배포·삭제·비밀값·
  프로덕션 접근을 하지 않는다** — 그런 요청은 사람에게 확인한다.
- 리더가 작업 중일 때 send 하면 끼어들기가 된다. 급하지 않으면 파일에 쓰고 한 줄만.

## 3. 작업 단위

- 티켓 ID 는 백로그의 `T-NN`. 브랜치 `agy/T-NN-짧은이름`, 워크트리 `~/project/comicai-wt/T-NN-짧은이름`.
- 워크트리 생성과 `pnpm install` 은 **리더가** 해서 넘긴다.
- **메인 워크트리(`~/project/comicai`)는 리더 전용.** 조수·워커는 읽기만 한다.
- 티켓마다 만질 영역을 적는다. 영역이 겹치는 티켓은 동시에 돌리지 않는다.

## 4. 규칙

**금지** — 자기 워크트리 밖 수정 · `git push` · `docker` · dev 서버 · `.env` 열기·값 출력 ·
`ssh mini`(프로덕션) · 리더 승인 없는 의존성 추가 · 티켓에 없는 Prisma 마이그레이션.

**필수**

- 커밋: 한국어, `feat|fix|refactor|docs|chore(영역): 요약`. **`Co-Authored-By` 줄 금지.**
  본문 마지막 줄 `관련 docs 갱신 필요 여부: 필요|없음|TODO`.
- 코드를 바꾸면 해당 `docs/*.md` 의 `path:line` 인용을 고친다(매핑은 `CLAUDE.md`).
- 검사 — 바꾼 패키지만 돌려도 된다(`pnpm --filter <패키지> test`):
  - 항상 `pnpm typecheck` · `pnpm lint` · 테스트 · `pnpm verify:docs`
  - 설정·인프라(`env-profile.json`, `infra/**`, `scripts/compose.sh`)를 건드리면 `pnpm env:check` 도
- 주석은 한국어, "무엇" 보다 "왜". 파일의 기존 문체를 따른다.
- `.env` 와 `env-profile.json` 중 어디에 둘지는 `docs/05-infra-ops.md` §5 "어디에 둘지 판단하는 법".

## 5. 완료 보고 — 다섯 칸, `_mail/T-NN/report.md`

1. 바꾼 파일
2. 커밋 해시
3. 돌린 검사와 결과 (명령 그대로)
4. **확인하지 못한 것** — docker 가 필요해 통합 테스트 못 돌림, 브라우저로 안 봄 등
5. 판단이 필요한 것

"다 됐다" 만 쓰면 반려다. **코드를 읽고 추정한 것은 "추정" 이라고 쓴다** — 확인한 것처럼 쓴
주장이 틀리면, 리더가 그 보고 전체를 다시 확인해야 한다.

## 6. 리더의 검증

diff 를 읽는다 → typecheck·lint·test·verify:docs → 필요하면 통합 테스트·docker 빌드·브라우저 →
통과하면 fast-forward 머지 → push → 필요하면 맥미니 배포. 반려하면 이유를 `_mail/T-NN/` 에 남긴다.

## 7. 막혔을 때

추측으로 밀고 나가지 않는다 → `[agy][차단]`. 스펙이 모호하면 가장 보수적인 해석으로 하고 보고에 적는다.
