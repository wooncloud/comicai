# 남은 일

> v1 — 2026-09-21 — 리더 초안을 조수가 검토해 확정. 규약은 [`../../AGENTS.md`](../../AGENTS.md).

끝난 항목은 지우고 맨 아래 "최근 끝난 것" 에 한 줄 남긴다.
담당: **사람**(사장님) / **리더**(Claude) / **agy**(워커·조수). 크기: S(<1h) · M(반나절) · L(하루+).

## 진행 순서

```
지금 ─── (개발 티켓 없음 — 사람 몫 O-05 대기)
결정 대기 ── O-05 사업자 정보가 오면 T-12 빈칸 채워 머지 (초안은 브랜치 `agy/T-12-legal-draft`, 화면 확인 끝)
```

동시에 도는 워커는 최대 2개다.

## A. 사람 — 계정이나 결정이 필요

| ID   | 할 일                                                                                     | 비고                           |
| ---- | ----------------------------------------------------------------------------------------- | ------------------------------ |
| O-03 | 백업 원격 사본 목적지(R2·B2 등) + 자격 증명                                               | 지금은 원본과 같은 디스크      |
| O-04 | 백업 실패 알림 웹훅(Slack·Discord)                                                        |                                |
| O-05 | 약관·처리방침 — 자리표시자, §5 의 사실과 다른 일일 상한, 결제·환불 조항, 사업자 정보      | 돈을 받기 전 필수. 초안은 T-12 |
| O-06 | ~~가격·환불 정책~~ **결정(2026-09-21)**: 가격 유지, 환불은 미사용 토큰·구매 7일 이내      | T-12 초안에 반영               |
| O-07 | ~~문구~~ **결정(2026-09-21)**: `충전 50토큰` 식 한글 상품명, 운영자 id 대신 `운영자 조정` | → T-02                         |

## B. 리더

| ID   | 할 일                                                                                                                                                                                         | 크기 |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| L-05 | 풀 테스트에서 **확인하지 못한 것** — ① API 를 내렸을 때 에디터 잔액 배지가 사라지는지(코드로만 확인: `token-balance.tsx` 가 `data` 없으면 `null` 반환) ② F 의 BYOK 표시(`FEATURE_API_KEYS=1`) | S    |
| L-04 | 각 T 티켓 검증·머지·맥미니 배포                                                                                                                                                               | 계속 |

## C. 개발 — agy

| ID   | 할 일                                                                                                                            | 영역 (겹침 방지)  | 크기 |
| ---- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ---- |
| T-12 | 약관·처리방침 유료 서비스 기준 초안. 가격 유지·환불(미사용·7일) 반영, 사업자 정보는 빈칸. 사람이 확정하기 전에는 머지하지 않는다 | `web/app/(legal)` | M    |

**겹침**: T-03·T-04 는 둘 다 `editor/tldraw` — 순서대로. T-02·T-09 는 `settings/billing` 화면이 겹친다.
T-10 은 여러 파일을 건드리므로 편집기 티켓과 동시에 돌리지 않는다.

## 반려한 제안

| 제안                                                | 이유                                                                                                                                                                  |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06 저장 뒤 재조회를 응답 DTO 로 대체              | **보류** — 얻는 것이 작고(디바운스 1.5초, 그 종류만, 수 KB, presign 은 로컬 계산) 막 안정시킨 동기화 엔진을 건드린다. `90-decisions/0008` 에 다시 볼 조건과 설계 요지 |
| S3_ENDPOINT·REDIS_URL·OAuth CLIENT_ID 를 프로파일로 | 각각 컨테이너 주소, 비밀이 들어갈 URL, 비밀과 짝인 값 — `docs/05-infra-ops.md` §5 "어디에 둘지 판단하는 법"                                                           |
| 플랫폼 키 빈 문자열 방어 (`model-credentials.ts`)   | 이미 되어 있다 — `platformKeyFor` 가 `nonEmpty(raw?.trim())` 을 쓴다. 근거로 든 줄은 인터페이스였다                                                                   |

## 최근 끝난 것

- 2026-09-26 프로젝트 전체 정리(`/simplify`, 재사용·단순화·효율·깊이 네 리뷰) — 5커밋(`5b87e89`…`6d3df75`). **리뷰가 찾은 회귀 넷을 먼저 고침**: 한 화 안 페이지 순서를 바꾸면 다른 화 페이지가 캐시에서 사라짐, 색 고르개가 끄는 동안 매번 저장(배경 PATCH 수십 개), 컷 테두리 색이 셰이프 저장에 덮여 사라짐, 화 삭제 시 저장소 청소 누락. 정리 과정에서 둘 더 — 말풍선 편집 칸이 React 가 그린 줄을 덮어쓰고 Esc 뒤 원문이 두 벌 보이던 것, 자유 텍스트가 PNG 에서 상자 밖으로 뻗던 것. 그 밖에 인스펙터 셰이프 구독, 내보내기 루프 하나, DTO 를 스키마에서 파생(말풍선 DTO 에 대사 필드가 없었음), presign 5분 창(저장마다 컷 그림 재다운로드). 건너뛴 것: PDF 를 JPEG 로 담기(출력이 바뀜), 웹이 안 쓰는 옛 엔드포인트 삭제(외부 클라이언트 여부 미확인), 재정렬 검사 헬퍼(호출부마다 예외 코드가 달라 줄지 않음) (리더)
- 2026-09-25 화(話) 도입과 내보내기 재편 — 프로젝트>화>페이지 그루핑(데이터·API·화면), 페이지 크기 프리셋을 웹툰/인스타/출판으로, 화 단위 내보내기(웹툰은 세로 이어 붙이기 + 16384px 경계 분할, 나머지는 한 장씩), 받는 방법에 ZIP·PDF 추가. ZIP 은 의존성 없이 직접 쓰고(STORE, `node:zlib` crc32) PDF 만 `pdf-lib`. 운영 마이그레이션 전 수동 백업 후 화 7개·페이지 14장 이관 확인, 운영에서 ZIP(CRC)·PDF(384×288pt@150dpi) 실물 검증 (리더)
- 2026-09-25 L-03 풀 테스트 — `50-owner/02-verify.md` 를 로컬 스택에서 브라우저(Playwright)로 한 바퀴. A-1·A-2·B-1·B-3·B-4·B-5·C-1·C-2·C-3·D-1·D-2·D-3·E-1·E-3·F(더 보기) 통과. **버그 두 건을 찾아 같은 날 고쳐 배포**: ① Gemini 비율 400 으로 컷이 한 장도 안 그려지던 것(`bed2df4`) ② 생성 실패 문구에 서버 원문 `no gemini key` 가 노출되던 것(`2e3fe0b`). 가입 메일은 yopmail 로 실제 수신까지 확인했고, 운영에서도 가입→인증메일→설정집→컷 생성(12초)→내보내기를 통합 확인 (리더)
- 2026-09-25 O-11 DB 비밀번호 교체 — 사장님이 고르려던 값에 URL 인코딩이 필요한 기호가 있어(compose 가 `DATABASE_URL` 을 그 값으로 조립한다) URL 안전한 40자로 새로 발급. 덤프 → `ALTER USER` → `.env` 두 줄 → migrate → api·worker·backup 교체, 로그인 401·백업 `pg_dump ok` 까지 확인. **유닉스 소켓과 127.0.0.1 은 `trust` 라 비밀번호 확인이 무의미하다**는 걸 두 번 헛짚고 알아냄 — 런북을 `docs/05-infra-ops.md` §8.3 에. O-09(옛 `.env.bak`)도 함께 삭제 (리더)
- 2026-09-25 O-01 Resend 도메인 인증 — `comic.wooncloud.com` (Tokyo `ap-northeast-1`) 등록, Cloudflare 에 DKIM·SPF CNAME 2개·DMARC 추가, Resend `Domain verified`. `EMAIL_FROM` prod 를 `no-reply@comic.wooncloud.com` 으로 (리더, 오르카 컴퓨터 유즈로 화면 조작. Resend 의 "Cloudflare Auto configure" 는 계정 권한을 통째로 넘기게 되어 쓰지 않음)
- 2026-09-22 L-02 복구 리허설 — 맥미니에서 격리된 임시 컨테이너로 DB 덤프 복원(1초, 8개 테이블 행 수·잔액=원장 일치), MinIO 복구 명령 시험. 런북을 `docs/05-infra-ops.md` §3.5 에 (리더)
- 2026-09-22 T-08 문서 인용 드리프트 **121 → 0** (4회 나눠서, agy). 번호만 옮기지 말고 주장을 코드로 확인하라는 조건으로, **사실과 달라진 문장 6개**를 함께 고침 — 페이지 PATCH 가 order 를 받는다(안 받는다), 렌더 UI 에 취소 버튼이 없다(있다), 토큰제 이전의 키 조회·종결 흐름 등
- 2026-09-22 T-13 랜딩 샘플 스크립트가 `.env` 를 공용 로더로 (`eee784a`, agy)
- 2026-09-22 T-09 운영자 지급 뒤 사용자 화면 자동 갱신 — 토큰 쿼리만 포커스 복귀 시 재조회, 입금 대기 중에만 60초 주기 조회 (`e5cc2fb`, agy)
- 2026-09-22 T-10 knip 정리 — 미사용 export 20·파일 2, 의존성 2개 제거(리더 판단), **CI 에 knip 추가** (`081520a`)
- 2026-09-21 T-05 `docs/03-frontend.md` 에 토큰·충전·`/admin` 화면, 쿼리 키 표 5→12 (`e579262`, agy)
- 2026-09-21 T-03 말풍선·텍스트·직선 앞뒤 순서 — 인스펙터 버튼·단축키·우클릭이 한 저장 경로로, 새로고침 유지 (`6b473cd`, agy). 리더 브라우저 검증에서 **기존 버그 두 건**을 찾아 같은 티켓에서 고침: 새로 그린 컷이 서버에서 온 말풍선·텍스트를 덮어 클릭이 안 되던 것, 컷에서 순서 단축키를 누르면 컷이 위로 올라가던 것
- 2026-09-21 T-14 배포 안전장치 — 빌드 → 인프라 확인(`--no-recreate`) → 떠 있는 DB 에 마이그레이션 → 앱 교체. 리더가 로컬 docker 로 오늘 사고를 재현해 "컨테이너 불변·사이트 유지" 확인, 프로덕션 첫 적용 71초 성공 (`253f33d`, agy + 리더: DB 가 꺼져 있으면 매번 실패하던 빈틈 수정)
- 2026-09-21 **사고**: 프로덕션 약 2분 다운(22:19:52–22:21:57 KST). `.env` 의 `POSTGRES_PASSWORD` 만 바뀌어 migrate 가 DB 인증 실패 → 배포가 앱 컨테이너를 먼저 지운 뒤라 복구할 게 없었다. 비밀번호를 DB 값으로 되돌려 복구. 재발 방지 T-14
- 2026-09-21 러너 배포가 20분 멈추던 원인 — launchd 아래 docker 자격 증명 헬퍼가 키체인을 기다림. 러너 전용 `DOCKER_CONFIG`(credsStore 없음)로 해결, 첫 자동 배포 74초 성공 (리더)
- 2026-09-21 T-04 동기화 훅 역방향 투영 통합 — 소스 −91줄, 웹 테스트 28→50, 브라우저로 생성·이동·저장·새로고침 확인 (`01d36e4`, agy 설계→리더 검토→단계별 구현)
- 2026-09-21 T-07 사용자 생성·가입 지급을 `UsersService` 로 (`830aaff`, agy + 리더: 약관 동의 시각 기본값 제거)
- 2026-09-21 T-02 원장 표기 — `충전 50토큰`·`운영자 조정`·`그림 생성 (Gemini)`, 옛 기록까지, DTO 에서 memo 제거 (`c4b4825`, agy + 리더 검토 2회)
- 2026-09-21 OpenAI·Gemini 키 설정·유효성 확인, Resend 키 설정 (사람)
- 2026-09-21 O-02 배포 러너를 맥미니로 이전, deploy 워크플로 재활성화 (리더)
- 2026-09-21 T-11 `compose.sh` 가 nvm 의 node 도 찾는다 (`004e6b1`·`1950a81`, agy — 리더가 줄이라고 한 번 돌려보냄)
- 2026-09-21 T-01 `/admin` 첫 문단 바로잡음 (`eeca6c6`, agy — 첫 시도는 백그라운드 함정으로 헛돌았다)
- 2026-09-21 O-10 맥북 옛 배포 삭제 — DB 덤프·MinIO 는 `~/project/comicai-archive/2026-09-21-macbook-old-deploy/` 에 보관 (`comicai-deploy` 폴더는 O-02 뒤에 삭제)
- 2026-09-21 `.env.bak*` 를 무시 목록에 (`649b7f9`)
- 2026-09-21 L-01 협업 규약 확정 (`AGENTS.md`)
- 2026-09-21 `.env` 배치 규칙 문서화 (`ea26446`, agy)
- 2026-09-21 백업 이미지: 410 과 arm64 불일치 수정, 맥미니 반영 (`1d4e960`, agy)
- 2026-09-21 맥미니 CORS 장애 수정 — 프로덕션 도메인을 `env-profile.json` 으로 (`46eebd7`)
