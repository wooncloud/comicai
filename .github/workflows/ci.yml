name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # JS 액션 런타임을 Node.js 24 로 강제. v4 액션들이 내부적으로 node20 을 쓰는데
  # 2026-09-16 부터 runner 에서 node20 이 제거되므로 미리 24 로 opt-in.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    name: lint / typecheck / test
    runs-on: [self-hosted, comicai]
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.12.0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages (types/db/events/adapters)
        run: pnpm -r --filter "./packages/*" build

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

  deploy:
    name: deploy to prod
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: [self-hosted, comicai]
    timeout-minutes: 20
    steps:
      - name: Pull & restart prod compose
        working-directory: ${{ secrets.PROD_REPO_PATH }}
        run: |
          git fetch --prune origin
          git reset --hard origin/main
          docker compose -f infra/compose/full.yml --env-file .env --profile tunnel --profile backup up -d --build --force-recreate web api worker
